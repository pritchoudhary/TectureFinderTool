namespace TextureTool
{
    internal enum EHeaderColumnId
    {
        TextureName = 0,
        TextureType,
        NPot, // Non Power of two
        MaxSize, // Max texture size
        GenerateMips, // Generate Mip Maps
        AlphaIsTransparency,
        TextureSize,
        DataSize,
    }
}
